#include<iostream>
using namespace std;

int n=0;
int main()
{
    int i = 1;
    int a[11];
    while (i<=11&&a[i-1]!=0)
        {
            cin >> a[i-1];
            if(a[i-1==0]);
            if(a[i-1]!=0)
            {
                a[i-1]/=2;
                cout << a[i-1] << endl;
                ++i;
            }

        }
}
